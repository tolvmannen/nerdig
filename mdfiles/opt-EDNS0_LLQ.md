## Long Lived Queries
---
<br/>

Thid option is an (Apple) extension to unicast DNS that enables a client to 
issue long-lived queries that allow a unicast DNS server to notify clients 
about changes to DNS data.
