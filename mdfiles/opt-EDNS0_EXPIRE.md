## EDNS Expire
---
<br/>

The EDNS EXPIRE option is used to prevent secondary name servers from keeping
stale zone data in the event that the primary nameserver is unreachable, but
other secondaries are not.

